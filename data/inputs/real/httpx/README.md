`data/inputs/real/httpx` is the canonical HTTPX real-input directory.
The only canonical READY evidence file expected here is `httpx-hosts.json`.

Current contents are placeholders only.
They preserve structure for the execution contract and must not be treated as READY evidence.
