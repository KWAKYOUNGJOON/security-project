# normalized(정규결과)

Path
- engagements\sample-folder\scans\normalized

Description
Normalized scan outputs aligned to the shared project schema.

Current implementation scope
- Web

Target scope
- Web + API + Server

Notes
- Actual folder names use English only.
- Documentation may use the English(한글) notation.
- Stabilize the web workflow first, then expand to API and server support.